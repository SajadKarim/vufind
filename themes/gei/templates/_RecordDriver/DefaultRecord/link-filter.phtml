<?
	$uri = $_SERVER['REQUEST_URI'];
	$uri2 = $this->url('search-results');
	if (strrpos($uri, $uri2) === false) {
		$uri = $uri2.'?';//we are not on a result page
	}else{
		$uri = $uri.'&';
	}
	$lf1 = $this->lookfor[0];
	$lf2 = $this->lookfor[1];
?>
<?=$uri?>filter[]=<?=urlencode($lf1)?>:%22<?=urlencode($lf2)?>%22